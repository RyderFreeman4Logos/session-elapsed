[project]
name = "session-elapsed"
version = "0.1.0"
description = "Hermes plugin: inject elapsed time and turn count into agent context"
license = "Apache-2.0"
requires-python = ">=3.11"

[build-system]
requires = ["setuptools>=64"]
build-backend = "setuptools.backends._legacy:_Backend"
